Pre requisite:
Node JS ande npm package must be installed.

To check whether you have them in your system type into the command line:
> node --version
> npm --version

Steps to run the web-app:

1. Install the dependencies by running this command:
> npm install

2. Run the app by running this command:
> npm run dev